import React from "react";

export const Balance = ({ bgColor }) => {
  return (
    <div className={`row-span-8 rounded-lg ${bgColor} p-3 h-[100%] text-white`}>
      Balance/Account
    </div>
  );
};
